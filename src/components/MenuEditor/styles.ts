import { styled } from '@ignite-ui/react'

export const Menu = styled('div', {
  background: '#fff',
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  borderBottom: '1px solid #c7c7c7',
})
